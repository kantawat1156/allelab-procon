# ดอกเบี้ย = เงินต้น(p) * อัตราดอกเบี้ย(r) * ระยะเวลากู้ยืม(t)
def simple_interest(p, r, t):
    interest = p * r / 100 * t #อัตราดอกเบี้ย(r)ต้องหาร 100 ก่อนใช้
    return interest + p #บวกเงินต้นเข้าไปด้วย(p)
